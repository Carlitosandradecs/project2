#include "heap.h"
#include "defns.h"
#include <iostream>
#include <fstream>
#include <vector>
#include <string>

    void readfile();
        string a;
        readfile.open("Occupation-Dist-All-1999.csv", ios::in); //open the file
        if (!readfile.is_open())
            std::cout << "ERROR: File Open" << '\n';


}

